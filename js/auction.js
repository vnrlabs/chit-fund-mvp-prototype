window.addEventListener('layout:ready',()=>{
 const bids=document.getElementById('bidRows');bids.innerHTML=ChitData.bids.map((b,i)=>`<div class="bid-row ${i===0?'winner':''}"><div class="bid-rank">#${b.rank}</div><div class="member-cell"><div class="avatar">${UI.initials(b.name)}</div><div><strong>${b.name}</strong><span>${b.id} · ${b.time}</span></div></div><div class="amount">${UI.money(b.bid)}</div>${i===0?'<span class="badge badge-success">Winner</span>':'<span class="badge badge-neutral">Valid</span>'}</div>`).join('');
 const p=document.getElementById('auctionPaymentRows');p.innerHTML=ChitData.payments.map(x=>`<tr><td><div class="member-cell"><div class="avatar">${UI.initials(x.name)}</div><div><strong>${x.name}</strong><span>${x.id}</span></div></div></td><td class="amount">${UI.money(x.due)}</td><td class="amount">${UI.money(x.paid)}</td><td><div class="payment-status"><span class="status-mark ${x.status==='Paid'?'status-paid':x.status==='Overdue'?'status-overdue':'status-pending'}"></span><strong>${x.status}</strong></div></td><td>${x.date}</td><td>${x.ref}</td><td>${x.status!=='Paid'?'<a class="action-link" href="payments.html">Record payment</a>':'<span class="muted">—</span>'}</td></tr>`).join('');
 document.getElementById('newBid').onclick=()=>UI.modal('Record auction bid',`<div class="form-grid"><div class="field"><label>Member</label><select class="select"><option>Divya M · MB-10123</option><option>Meena R · MB-10034</option></select></div><div class="field"><label>Bid / Thallupadi Amount</label><input class="input" value="₹2,15,000"></div></div><div class="callout callout-warning" style="margin-top:14px">This cycle is already finalized in the prototype. Production allows changes only under controlled correction rules.</div>`);
});
